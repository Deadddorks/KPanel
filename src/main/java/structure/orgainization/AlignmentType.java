package structure.orgainization;

/**
 * Used by several classes to tell whether a property has a set CONSTANT value, or if it scales based on a
 * PERCENT of its parents size
 * @author Deaddorks
 */
public enum AlignmentType
{
	CONSTANT,
	PERCENT
}
